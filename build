#!/bin/bash
# FILE AUTO-GENERATED. DON'T OVERWRITE MANUALLY.

set -e

DOCKERINDEX=${DOCKERINDEX_LOCAL:-"datt/"}

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
CURRENT_DIR="${DIR##*/}"

echo docker build -rm -t=$DOCKERINDEX$CURRENT_DIR:latest $DIR/.
